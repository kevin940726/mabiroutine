# Cloudflare Pages — static, no functions needed. This file is for `wrangler pages deploy dist` alternative.
name = "mabiroutine"
type = "javascript"
compatibility_date = "2024-11-01"

[site]
bucket = "./dist"
